# D7 — audit VM `netviet` bằng số MỚI (23/08/2026, 11:02 UTC)

Đo trực tiếp trên VM qua `gcloud compute ssh --tunnel-through-iap`. **Chỉ đọc** — không prune,
không sửa gì. Số cũ ở runbook §7 không được dùng lại.

## 1. Máy

| Chỉ số | Giá trị |
|---|---|
| uptime | 23 ngày 6:41 |
| load (1/5/15) | 1,79 / 1,61 / 1,35 |
| CPU | **2 nhân** |
| RAM | 7,8 Gi tổng · 4,1 Gi dùng · **3,6 Gi available** · **swap 0** |
| Đĩa `/` | 77 G tổng · 70 G dùng · **6,6 G trống · 92%** |

Load 1,79 trên 2 nhân ≈ **0,9 runqueue/nhân** — máy đang bận, chưa quá tải.

## 2. Đang chạy: 5 compose project, 17 container

`netviet-edge` (1) · `zalo-amico` (4) · `zalo-ultty` (4) · `zalo-ultty-gd1-test` (4) · `zalo-wata` (4)

RAM theo container (tổng ≈ **3,30 GB**):

| Loại | Mỗi cái | ×4 khách |
|---|---:|---:|
| flowise | 538–562 MiB | ≈ 2,16 GB |
| api | 90–116 MiB | ≈ 0,41 GB |
| web | 87–126 MiB | ≈ 0,41 GB |
| postgres | 60–65 MiB | ≈ 0,25 GB |
| edge gateway | 18 MiB | 0,02 GB |

## 3. RAM có đủ cho Hatchet không — **CÓ**

| Khoản | RAM |
|---|---:|
| Cụm engine (đo ở D6 trên compose thật) | 252 MiB |
| `workflow-worker-v1` | **CHƯA ĐO** — ước theo `api` cùng image thì cỡ 100–120 MiB, nhưng đó là **ƯỚC, không phải số đo** |
| Cộng ước tính | **≈ 0,35–0,38 GB** |
| Còn available sau đó | ≈ **3,2 GB** |

⇒ RAM **không phải cổng chặn**. Nhưng **swap = 0**, nên hết RAM là OOM-kill thẳng, không có vùng
đệm — đó là lý do không được ước rộng tay ở đây.

## 4. ⛔ ĐĨA LÀ CỔNG CHẶN — 92%, còn 6,6 GB

```
Images         82 tổng · 10 active · 68,11 GB · RECLAIMABLE 52,01 GB (76%)
Containers     21 tổng · 17 active · 1,11 MB
Local Volumes  10 · 312,1 MB · reclaimable 0
Build Cache    0
```

**Nguyên nhân đã xác định, không phải phỏng đoán: 40 bản KHÔNG TAG của
`flowise-3.1.4-deepseek-fix`, mỗi bản 9,29 GB.** Đây là các build cũ bị bản mới thay tag; chúng
chia sẻ layer nên tổng thực tế thu hồi được là **52,01 GB** như `docker system df` báo.

Thêm Hatchet cần **≈ 1,04 GB ảnh + 73 MB volume ≈ 1,1 GB** ⇒ đĩa sẽ lên **≈ 94%**, còn ~5,5 GB.

**Vì sao đó là CHẶN chứ không phải "chật một chút":** mỗi lần deploy một khách lại kéo về một ảnh
app + flowise mới và **để lại bản cũ không tag**. Với 4 khách đang chạy, VM đang tiêu đĩa theo mỗi
lần deploy mà không có gì dọn. Deploy Hatchet lên nền 92% là **đặt một tính năng mới lên một quả
bom hẹn giờ đã có sẵn** — và khi đĩa đầy thì hỏng cái chết trước sẽ là PostgreSQL của **khách đang
chạy**, không phải Hatchet.

### Dọn được không, và có an toàn không — ĐÃ KIỂM

`rollback.sh:64` chạy `docker compose pull api web flowise` — tức rollback **kéo lại từ Artifact
Registry theo digest**, **không** dựa vào ảnh còn nằm trên máy.

Đã kiểm AR: repo `netviet/flowise-3.1.4-deepseek-fix` giữ **47 bản có tag** (mỗi bản gắn git SHA).

⇒ Xoá các ảnh **không tag, không container nào dùng** trên VM **không làm mất đường rollback**.

> **KHÔNG THỰC HIỆN — đã chọn đường khác.** Xem §6.

## 6. ĐÃ XỬ LÝ: nới đĩa 80 → 200 GB, KHÔNG downtime (23/08/2026, 11:2x UTC)

Người dùng cho phép nâng VM, không phải cân nhắc chi phí. Chọn **nới đĩa**, **không** đổi machine
type — và đó là một khác biệt quan trọng:

| Cách | Downtime | Có cần không |
|---|---|---|
| **Nới đĩa `pd-balanced`** | **KHÔNG** — nới nóng được, rồi `growpart` + `resize2fs` online | ✅ đây là thứ thiếu |
| Đổi machine type | **CÓ** — phải tắt máy ⇒ ngắt **cả 4 khách** | ❌ RAM còn 3,6 Gi, CPU load 0,9/nhân — không thiếu |

```
gcloud compute disks resize netviet --zone asia-southeast1-b --size 200
sudo growpart /dev/sda 1     -> CHANGED: partition=1 ... new: size=417331167
sudo resize2fs /dev/sda1     -> on-line resizing required; now 52166395 (4k) blocks
```

| | Trước | Sau |
|---|---|---|
| `/` | 77 G · còn **6,6 G** · **92%** | 193 G · còn **123 G** · **37%** |

**Xác nhận không downtime bằng bằng chứng, không bằng lời:** sau khi nới, cả **17 container** vẫn
`healthy` và **uptime không đổi** (`Up 10 days`, `Up 7 days`, `Up 6 days`, `Up 42 hours`, …) —
không cái nào khởi động lại.

**Vẫn còn nợ (không chặn D8, nhưng đừng quên):** 40 ảnh không tag × 9,29 GB vẫn nằm đó, và **mỗi
lần deploy bất kỳ khách nào lại thêm một cái nữa**. Nới đĩa mua thêm thời gian chứ không sửa cái
tích luỹ. Việc dọn vẫn an toàn theo phân tích trên (AR giữ 47 bản có tag, `rollback.sh:64` kéo lại
theo digest) — để lại cho một phiên vận hành riêng, làm có chọn lọc, **không** `prune -a` mù.

## 7. Kết luận cho D8

| Cổng | Kết quả |
|---|---|
| RAM | ✅ đủ (≈3,2 GB còn lại sau khi thêm) |
| CPU | ✅ 2 nhân, load 0,9/nhân |
| Cổng mạng | ✅ không đụng — cụm Hatchet không publish cổng nào |
| **Đĩa** | ✅ **37%, còn 123 GB** sau khi nới 80→200 GB (§6) |

**D7 XANH.** Thêm Hatchet ≈1,1 GB ⇒ đĩa lên ~38%, còn ~122 GB.
