# POC-3 — review handshake sandbox

The review-gate workflow appends one line here to move the branch head, so the stale-SHA
test is performed against a real push rather than a simulated one.

This is the only file that workflow may write.

<!-- automation appends below this line -->
